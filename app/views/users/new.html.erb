<% content_for :context_menu do %>
  <div class="contextMenuItem">
    <%= link_to "Cancel", users_path if policy(User).index? %>
  </div>
<% end %>
<div>
  <h2>Create User</h2>
  <% @edit_mode = true %>
  <% @action = 'create' %>
  <% @submit_text = 'Create User' %>
  <%= render 'user' %> 
</div>

