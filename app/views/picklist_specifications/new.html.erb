<% content_for :context_menu do %>
  <div class="contextMenuItem">
    <%= link_to("Cancel", {action: 'index'}) if policy(PicklistSpecification).index? %>
  </div>
<% end %>

<%= render(partial: 'show_picklist_specification') %>
