<% content_for :context_menu do %>
  <div class="contextMenuItem">
	  <%= link_to("Cancel", {:action => 'show', :id => @box.id}) %>
	</div>
<% end %>
<div>
  <h2>Edit Box</h2>
    <% @edit_mode = true %>
    <% @action = "update" %>
    <% @submit_text = "Update Box" %>
    <%= render(:partial => "box") %>
</div>
