<% content_for :context_menu do %>
  <div class="contextMenuItem">
    <%= link_to("Advanced Search", :controller => 'search', :action => 'index') if policy(:search).advanced_search? %>
  </div>
  <div class="contextMenuItem">
    <%= link_to("Workflow History Report", controller: 'physical_objects', action: 'contained') if policy(PhysicalObject).contained? %>
  </div>
<% end %>
<div>
  Choose a report from the menu options above.
</div>
