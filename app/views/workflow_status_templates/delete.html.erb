<% content_for :context_menu do %>
  <div class="contextMenuItem">
    <%= link_to("Cancel", {action: 'show', id: @workflow_status_template.id}) %>
  </div>
<% end %>
<div>
  	<% @submit_text = "Delete Workflow Status" %>
  	<% @edit_mode = false %>
  	<% @action = 'destroy' %>
   	<%= render(:partial => "new_physical_object_workflow_status_form") %>
</div>