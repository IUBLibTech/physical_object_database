<% content_for :context_menu do %>
  <div class="contextMenuItem">
	  <%= link_to("Cancel", {:action => 'index'}) if policy(Bin).index? %>
	</div>
<% end %>
<%= error_messages_for(@bin) %>
<div>
  <h2>Create Bin</h2>
    <% @edit_mode = true %>
    <% @action = "create" %>
    <% @submit_text = "Create Bin" %>
    <%= render(:partial => "bin") %>
</div>
