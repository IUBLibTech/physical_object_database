<% content_for :context_menu do %>
  <div class="contextMenuItem">
	  <%= link_to("Cancel", {:action => 'show', :id => @bin.id}) %>
	</div>
<% end %>
<div>
  <h2>Edit Bin</h2>
    <% @edit_mode = true %>
    <% @action = "update" %>
    <% @submit_text = "Update Bin" %>
    <%= render(:partial => "bin") %>
</div>