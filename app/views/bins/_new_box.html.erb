<h2>New Box</h2>

<%= form_for(:box, :url => {:action => 'create_box', :id => @bin.id}) do |f| %>
	<table>
		<tr>
			<th>MDPI barcode</th>
			<td>
				<%= f.text_field(:mdpi_barcode) %>
			</td>
		</tr>
	</table>
	<%= f.submit('Create New Box') %>
<% end %>