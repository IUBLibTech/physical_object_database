<% content_for :context_menu do %>
  <div class="contextMenuItem">
    <%= link_to "Cancel", units_path if policy(Unit).index? %>
  </div>
<% end %>
<div>
  <h2>Create Unit</h2>
  <% @edit_mode = true %>
  <% @action = 'create' %>
  <% @submit_text = 'Create Unit' %>
  <%= render 'form' %> 
</div>

