<% content_for :context_menu do %>
  <div class="contextMenuItem">
    <%= link_to("Advanced Search", :controller => 'search', :action => 'index') if policy(:search).advanced_search? %>
  </div>
  <div class="contextMenuItem">
    <%= link_to("Contained Objects Report", controller: 'physical_objects', action: 'contained') if policy(PhysicalObject).contained? %>
  </div>
<% end %>
Download full search results as: <%= link_to('Excel (XLS) File', advanced_search_search_index_path(params.merge(format: :xls)), method: :post) if policy(:search).advanced_search? %>
<h2>Physical Objects<%= " [#{@results_count}]" if @search_mode %></h2>
<%= render 'physical_objects/physical_objects_table' %>
