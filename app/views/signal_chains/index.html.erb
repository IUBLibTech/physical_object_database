<% content_for :context_menu do %>
  <div class="contextMenuItem">
    <%= link_to "Create Signal chain", new_signal_chain_path if policy(SignalChain).new? %>
  </div>
    <div class="contextMenuItem">
  	<%= link_to "Devices", machines_path if policy(Machine).index? %>
  </div>
<% end %>
<div>
  <h2>Signal chains</h2>
  <%= render 'signal_chains_table' %>
</div>
