<% content_for :context_menu do %>
  <div class="contextMenuItem">
    <%= link_to "Edit", edit_signal_chain_path(@signal_chain) if policy(@signal_chain).edit? %>
  </div>
  <div class="contextMenuItem">
  <%= link_to "Delete", signal_chain_path(@signal_chain), method: :delete, data: { confirm: 'Are you sure you want to delete this Signal chain?' } if policy(@signal_chain).destroy? %>
  </div>
  <div class="contextMenuItem">
    <%= link_to "Signal chains Index", signal_chains_path if policy(SignalChain).index?%>
  </div>
<% end %>
<h2>Signal chain</h2>
<%= render partial: 'signal_chain' %>
<h2>Processing steps</h2>
<%= render partial: 'processing_steps' %>
<h2>Add a Processing step</h2>
New steps are added to the end of the current chain.
<%= render partial: 'add_processing_step' %>
<h2>Reorder Processing steps</h2>
Any gaps created by removing intermediate steps will be eliminated after submission if dragged to the final position.
<%= render partial: 'reorder_processing_steps' %>
