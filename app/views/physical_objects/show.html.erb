<% content_for :context_menu do %>
  <div class="contextMenuItem">
    <%= link_to("Edit", {:controller => 'physical_objects', :action => 'edit', :id => @physical_object.id}, :class => 'action edit') %>
  </div>
  <div class="contextMenuItem">
  <%= link_to("Delete", physical_object_path(@physical_object.id), method: :delete, data: {:confirm=>'Are you sure you want to delete this Physical Object?'})%>
  </div>
  <div class="contextMenuItem">
    <%= link_to("Split Record", {:controller => 'physical_objects', :action => 'split_show', :id => @physical_object.id}, :class => 'action split')%>
  </div>

  <div class="contextMenuItem">
    <%= link_to("Create New Physical Object", :action => 'new') %>
  </div>
  <div class="contextMenuItem">
    <%= link_to("View Imported Spreadsheets", spreadsheets_path) %>
  </div>
  <div class="contextMenuItem">
    <%= link_to("Upload Spreadsheet", upload_show_physical_objects_path) %>
  </div>
  <div class="contextMenuItem">
    <%= link_to "View Workflow History", workflow_history_physical_object_path(@physical_object) %>
  </div>
<% end %>

<%= render(partial: "physical_object_form", display_assigned: true) %>
