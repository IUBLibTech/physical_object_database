<p id="notice"><%= notice %></p>

<p>
  <strong>Content:</strong>
  <%= @message.content %>
</p>

<%= link_to 'Edit', edit_message_path(@message) if policy(@message).edit?%> | 
<%= link_to 'Back', messages_path if policy(Message).index? %>
