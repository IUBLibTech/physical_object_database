<% content_for :context_menu do %>
  <div class="contextMenuItem">
	  <%= link_to("Create Batch", {:action => 'new'}) if policy(Batch).new? %>
	</div>
<% end %>
<h2>Batches</h2>
<%= render 'batch_table' %>
